[project]
name = "common"
version = "0.1.0"
description = "Shared models and utilities for the monorepo"
readme = "README.md"
authors = [{name = "Socratic Developers", email = "dev@example.com"}]
requires-python = ">=3.11"
dependencies = ["pydantic>=2.6.0"]

[project.optional-dependencies]
dev = ["pytest>=8.0.0"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

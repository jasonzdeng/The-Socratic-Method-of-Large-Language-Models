# Frontend Application

The frontend is a Next.js 14 application configured with TypeScript and Tailwind CSS. Install
dependencies and run the development server with:

```bash
npm install
npm run dev
```
